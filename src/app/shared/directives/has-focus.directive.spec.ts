import { HasFocusDirective } from './has-focus.directive';

describe('HasFocusDirective', () => {
  it('should create an instance', () => {
    // const directive = new HasFocusDirective();
    // expect(directive).toBeTruthy();
  });
});
